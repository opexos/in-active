CREATE TABLE CC_LOG (
  ID INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL, 
  CC_DEVICE_ID INTEGER NOT NULL,
  LOG_DATE TIMESTAMP DEFAULT SYSDATE NOT NULL,
  RESULT D_GET_CONFIG_RESULT NOT NULL,
  ERROR_LOG CLOB,
  CONSTRAINT CC_LOG_PK PRIMARY KEY (ID),
  CONSTRAINT CC_LOG_REF_CC_DEVICES FOREIGN KEY (CC_DEVICE_ID) REFERENCES CC_DEVICES(ID) ON DELETE CASCADE
)



DROP TABLE CC_LOG


alter table cc_log drop constraint CC_LOG_REF_CC_DEVICES

alter table cc_log add CONSTRAINT CC_LOG_REF_CC_DEVICES FOREIGN KEY (CC_DEVICE_ID) REFERENCES CC_DEVICES(ID) on delete cascade




