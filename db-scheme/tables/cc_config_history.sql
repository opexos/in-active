CREATE TABLE CC_CONFIG_HISTORY (
  ID INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL, 
  CONFIG_DATE TIMESTAMP DEFAULT LOCALTIMESTAMP NOT NULL,
  CONFIG CLOB NOT NULL,
  WHEN_CHECKED TIMESTAMP,
  WHO_CHECKED INTEGER,
  CC_DEVICE_ID INTEGER NOT NULL,
  CONSTRAINT CC_CONFIG_HISTORY_PK PRIMARY KEY (ID),
  CONSTRAINT CC_CONFIG_HISTORY_REF_CC_DEVICES FOREIGN KEY (CC_DEVICE_ID) REFERENCES CC_DEVICES(ID) ON DELETE CASCADE,
  CONSTRAINT CC_CONFIG_HISTORY_REF_USERS FOREIGN KEY (WHO_CHECKED) REFERENCES USERS(ID)
)



DROP TABLE CC_CONFIG_HISTORY


ALTER TABLE CC_CONFIG_HISTORY DROP CONSTRAINT CC_CONFIG_HISTORY_REF_CC_DEVICES

ALTER TABLE CC_CONFIG_HISTORY ADD CONSTRAINT CC_CONFIG_HISTORY_REF_CC_DEVICES FOREIGN KEY (CC_DEVICE_ID) REFERENCES CC_DEVICES(ID) ON DELETE CASCADE




