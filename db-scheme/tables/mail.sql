CREATE TABLE MAIL (
  ID INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL, 
  RECEIVERS VARCHAR(10000) NOT NULL,
  SUBJECT VARCHAR(1000) NOT NULL,
  BODY CLOB NOT NULL,
  INSERTED TIMESTAMP DEFAULT LOCALTIMESTAMP NOT NULL,  
  PROCESSED TIMESTAMP,
  LAST_ERROR VARCHAR(10000),
  ERROR_COUNT INT)
  

drop table mail



