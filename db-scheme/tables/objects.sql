CREATE TABLE OBJECTS (
  ID INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL, 
  NAME D_VC NOT NULL,
  COMMENT D_VC_LONG,
  MAP_ID INTEGER NOT NULL,
  TYPE D_OBJECT_TYPE NOT NULL,
  CONSTRAINT OBJECTS_REF_MAPS FOREIGN KEY (MAP_ID) REFERENCES MAPS(ID),
  --CONSTRAINT OBJECTS_UNIQUE_NAME_IN_MAP UNIQUE (NAME, MAP_ID),
  CONSTRAINT OBJECTS_PK PRIMARY KEY (ID)
)

DROP TABLE OBJECTS

alter table objects drop constraint OBJECTS_UNIQUE_NAME_IN_MAP


