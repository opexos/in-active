CREATE TABLE M_OBJECTS (
  ID INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL, 
  NAME D_VC NOT NULL,
  COMMENT D_VC_LONG,
  MAP_ID INTEGER NOT NULL,
  CONSTRAINT M_OBJECTS_REF_MAPS FOREIGN KEY (MAP_ID) REFERENCES MAPS(ID),
  CONSTRAINT M_OBJECTS_UNIQUE_NAME_IN_MAP UNIQUE (NAME, MAP_ID),
  CONSTRAINT M_OBJECTS_PK PRIMARY KEY (ID)
)

DROP TABLE M_OBJECTS
