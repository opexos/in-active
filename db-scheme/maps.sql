CREATE TABLE MAPS (
  ID INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL, 
  NAME D_VC NOT NULL,
  COMMENT D_VC_LONG,
  CONSTRAINT MAPS_UNIQUE_NAME UNIQUE (NAME),
  CONSTRAINT MAPS_PK PRIMARY KEY (ID)
)


DROP TABLE MAPS
